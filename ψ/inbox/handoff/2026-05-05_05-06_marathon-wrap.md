# Handoff: arra-mcp marathon wrap

📡 Session: `5fd1aebf` | arra-mcp-installation-guide-oracle | spans 2026-05-04 21:21 → 2026-05-05 05:06

**Date**: 2026-05-05 05:06 +07
**Identity**: arra-mcp-installation-guide-oracle (renamed from sage-vector-fix on 2026-05-04)
**Federation tag**: `[m5:arra-mcp-installation-guide]`

## Context

This session was a 30-iteration cron-driven contribution chain to arra-oracle-v3. Started with an oracle-rename ceremony, ended with the indexer-CLI structurally complete and a wrangler deploy runbook documented. User cancelled the cron loop on wake, ran `/rrr`, now requesting `/forward`.

## What We Did

### Architecture shipped (arra-oracle-v3)

**Reranker chain** (3 PRs, all targeting alpha):
- #1098 — Python reranker sidecar (`services/reranker-py/`, FastAPI on :8765, bge-reranker-v2-m3)
- #1100 — `rerankCandidates()` helper (graceful-fallback TS client, 9 tests)
- #1101 — wire reranker into `combineResults()` (23 LOC, env-gated `ORACLE_RERANKER_URL`)

**Indexer M-chain** (5 PRs, M1→M5, mostly stacked):
- #1102 — M1: `indexing_jobs` table + 6 queue helpers (16 tests)
- #1103 — M2: worker loop (pure DI, 11 tests)
- #1104 — M3: daemon HTTP API + entrypoint (Hono, SSE, 14 tests)
- #1106 — M4: `arra-indexer` CLI (zero npm deps, 25 tests)
- #1107 — M5: `arra_learn` env-gated enqueue (10 LOC production diff, 4 tests)

**Standalone improvements**:
- #1097 — qwen3 dim fallback + instruction-prefix protocol (✅ MERGED)
- #1105 — `VectorDocument.vector` precomputed-vector field (eliminates daemon double-embed)

**Stats**:
- ~2,680 LOC total (production ~1,180, tests ~1,500)
- 70 hermetic tests across the indexer chain
- Combined indexer suite: M1(16) + M2(11) + M3(14) + M4(25) + M5(4) all green

### Empirical proof chain

- bge-m3 wins extended Thai bench 100/100/100 (paraphrase + distractors)
- qwen3:4b instruction-prefix patch: cross-lang 71% → 100% (+29 pts)
- SEA-LION-ModernBERT-600M LOSES on our use case despite SEA-BED leaderboard advantage (-36 pts cross-lang)
- Reranker smoke test: +14.3 pts R@1 cross-language
- Reranker real-corpus eval: +0 lift (production hybrid is saturated; bench-blind)
- Reranker latency: p50 167ms@C1, single-thread serializes hard, 2s timeout = load shedder

### Vault deliverables (this oracle)

- 12+ vault learnings in `ψ/memory/learnings/`
- 1 design doc: `ψ/lab/indexer-cli/DESIGN.md`
- 5 reusable Python benches in `ψ/lab/embedding-benchmark/`
- 1 deploy runbook: `DEPLOY-UI.md` at repo root
- 1 retrospective: `ψ/memory/retrospectives/2026-05/05/05.03_arra-mcp-marathon.md`
- 4 architectural memories at `~/.claude/projects/.../memory/`:
  - Don't fake the metric
  - Degrade gracefully over error
  - Anticipate branch catchup
  - Stigmergic disclosure

## Current State

**Stack** — all healthy as of 05:06:
- arra-oracle-v3 :47778 ✅
- ui-oracle studio :3000 ✅
- arra-reranker :8765 ✅

**This oracle's git**: clean except 2 untracked files (the `/rrr` retro + lesson, by design — vault files don't get `git add`'d).

**arra-oracle-v3 git**: on main, 9 feature branches I created still local + pushed.

**Cron**: cancelled (was `204e49b0` every-30m; user requested cancel on wake).

## Pending

- [ ] 9 PRs awaiting review on arra-oracle-v3 (#1098, #1100, #1101, #1102, #1103, #1104, #1105, #1106, #1107)
- [ ] M3 daemon's `upsertVector` re-embed inefficiency (waits for #1105 to land first)
- [ ] `arra-indexer scan <path>` subcommand (deferred from M4)
- [ ] `arra-indexer backfill --model X` subcommand (deferred from M4)
- [ ] Real-corpus eval with 50+ hand-labeled queries (current claim is 12-query smoke test)
- [ ] End-to-end smoke test for indexer M-chain (only have hermetic unit tests)
- [ ] M5 enqueue switch never run against a live daemon (architecture works in tests, end-to-end unverified)
- [ ] Wrangler deploy of any change (runbook in `DEPLOY-UI.md`, command copy-pasteable, awaiting OK)
- [ ] Alpha-vs-main reconciliation (M5 forks at the inline-embed point; maintainer judgment when alpha catches up)
- [ ] Build `bun run alpha-status` script (would surface alpha-vs-main drift at PR-open time)
- [ ] Commit retro + lesson to git (currently untracked because they're vault files; ψ vault commit policy says don't push)

## Next Session

- [ ] `/recap` to orient
- [ ] Check PR review status — if anything landed overnight, plan next wave
- [ ] Decide pace: continue chain (deferred subcommands, end-to-end smoke) OR pivot (different track entirely)
- [ ] Optionally: pull DESIGN.md into a public RFC for the indexer-CLI

## Key Files

- `ψ/lab/indexer-cli/DESIGN.md` — 7-milestone roadmap (M1-M5 shipped, M6/M7 deferred)
- `ψ/memory/learnings/2026-05-05_marathon-session-lessons.md` — what worked, what to do differently
- `ψ/memory/retrospectives/2026-05/05/05.03_arra-mcp-marathon.md` — full /rrr retro
- `DEPLOY-UI.md` — wrangler runbook for the 6 ui-oracle apps
- arra-oracle-v3 PR #1097 (merged) — entry point to the empirical chain
- All 9 open PR bodies cite each other; reading in PR-number order tells the story

## Cross-Branch Note

- `arra-oracle-v3` has stale `alpha` (behind main on schema 0007-0015 + Hono-vs-Elysia + arra_learn inline-embed)
- M5's PR #1107 will need maintainer judgment when alpha catches up (decide: remove inline embed when env=1, or keep both)
- All my indexer PRs use migration `0016` (renumbered from `0007` after I caught the collision)
